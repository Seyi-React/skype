    //  import { INewUser } from "@/type/type"
    //  import { ID } from "appwrite";
    //  import { account } from "./config";

     



    //  export async function  createUser = (user:INewUser): Promise<void>  => {
     
    //     try {
    //        const newAccount = await account.create (
    //         ID.unique(),
    //         user.email,
    //         user.password,
    //         user.username,
    //         user.name
    //        )

    //        if(!newAccount) throw error

    //        return newAccount;

    //     } catch (error) {
    //          console.log(error);
    //           return error;
    //     }
        
    //  }